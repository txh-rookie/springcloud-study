server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: serookie.com:8848
      config:
        server-addr: serookie.com:8848 #Nacos 作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: TEST_GROUP
